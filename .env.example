# Example environment variables for mockdata generator

# For local development with Docker Compose mock servers
# (Use these values when running the mock servers with docker-compose)
# Projects API runs on port 8080 (requires authorization)
# FGA API runs on port 8081 (no authorization required)
PROJECTS_URL=http://localhost:8080/projects
LFX_API_KEY=mock-api-key
FGA_API_URL=http://localhost:8081
FGA_STORE_ID=mock-store-id

# For development environment with default domains
# Projects API: lfx-api.k8s.orb.local
# FGA API: openfga.cluster.svc.local
# (Uncomment and use these when deploying to actual services)
# PROJECTS_URL=http://lfx-api.k8s.orb.local/projects
# LFX_API_KEY=your-api-key-here
# FGA_API_URL=http://openfga.cluster.svc.local:8080
# FGA_STORE_ID=your-store-id-here
